# @altairalabs packages (brand, atlas-tokens) are published to GitHub Packages,
# not npmjs. CI supplies NODE_AUTH_TOKEN; locally, `gh auth token` works.
@altairalabs:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
